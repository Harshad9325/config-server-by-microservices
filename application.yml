spring:
 datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password:
    
 jpa:    
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true   
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:4444/eureka 

okta:
  oauth2:
    issuer: https://dev-91066175.okta.com/oauth2/default
    audience: api://default 
